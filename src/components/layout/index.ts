/**
 * AttendSys Layout Component Library — Barrel Export
 *
 * Import from "@/components/layout" to access global layout components.
 *
 * Example:
 *   import { FullscreenLoader, FullscreenLoaderProvider, useFullscreenLoader } from "@/components/layout";
 */

export {
  FullscreenLoader,
  FullscreenLoaderProvider,
  useFullscreenLoader,
} from "./FullscreenLoader";
